# Hex / cube / sphere

Stay on target.

```
hex     = the slice     (neighbors in a plane)
cube    = the stack     (slices piled; a cell you can count)
sphere  = the rotation  (the stack allowed to turn)
```

These are **jobs**, not three substances. Same lattice, three readings.

## Hex — slice

The cut. D-408. Six neighbors. HEX-SPLIT floor plan.

A slice has a facing and an opposite. Three midlines. M4 freezes one.
You can print this. You can count 6. You cannot spin a slice and call it a world.

## Cube — stack

Slices piled until you have thickness. Cell → cube → Rubik on the GCAC ladder.
The stack is how 2D six-fold stands toward D-409 twelve-fold without pretending the plane was already a ball.

A cube has a hold belt in the middle of a layer: `1(0)1`. You do not divide Ground when you stack. You add a floor.

If stacking changes the dead-zone width, you stacked wrong — you divided the hold.

## Sphere — rotation

POINT-SPIN / GRAV-LAB lock. The stack is allowed to turn. 4π sheet. `lap`.

Sphere is not “smoother hex.” Sphere is **what the facing does when a write goes around**.
Parent proximity lock sits here: child facing stops walking relative to the parent line.

Quaternion is the engineering object for this job. Do not import Hamilton and call the lattice solved. Compare. (`POINT-SPIN`)

## Do not smash

| if you say | you are doing |
|---|---|
| hex = space | skipping the stack |
| cube = the world | skipping rotation |
| sphere = 12-clock | mixing D-411 jobs |
| stack by dividing hold | illegal |

Scale ladder (GRAV-LAB `SCALE_LADDER.md`) is how floors share a jamb.
This file is which **shape** is allowed to speak on which floor.

## Falsify

- A hex print behaves like a sphere lock with no stack in between — slice stole rotation.
- Stacking cells changes GCAC dead width — hold was not invariant.
- Sphere math (quat 4π) and lattice `lap` disagree on a closed trip — sheet dropped.
